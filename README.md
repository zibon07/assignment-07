#What is JSX, and why is it used?
-jsx is javascript+html like syntax!it is used to write UI inside javascript!
#What is the difference between State and Props?
-props pass data into component & state can be changed inside the component & changing state re-renders the UI!!!!
#What is the useState hook, and how does it work?
-useState hook lets a component remembers the data and update the ui when data changes!without react hook component would be static!
#How can you share state between components in React?
-i can share state using props ! i can share state from paarent to child through props driling etc!!
#How is event handling done in React?
-in react you add event handler like onClick,onChange etc then you pass a function in the event then the function does the work!!!
